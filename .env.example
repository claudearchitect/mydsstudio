# Anthropic API key — server-only. Read exclusively in route handlers
# (e.g. src/app/api/turn/route.ts, Workstream C). Never prefix with
# NEXT_PUBLIC_ and never import it into client components — that would ship
# it to the browser bundle.
ANTHROPIC_API_KEY=
